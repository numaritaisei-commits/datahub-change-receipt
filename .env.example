# The official DataHub SDK reads these values only in real DataHub mode.
# The free fixture judge demo does not load this file or require either value.
# Never commit a populated .env file.
DATAHUB_GMS_URL=http://localhost:8080
DATAHUB_GMS_TOKEN=
