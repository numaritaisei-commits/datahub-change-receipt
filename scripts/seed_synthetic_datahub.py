"""Seed the synthetic live-smoke graph into the configured ephemeral DataHub Core."""

from __future__ import annotations

from datahub_change_context.demo_seed import main

if __name__ == "__main__":
    raise SystemExit(main())
